minecraft_version=26.1.2
minecraft_version_range=[26.1,26.2)
neo_version=26.1.2.103
neo_version_range=[26.1.0,)
loader_version_range=[4,)

resource_pack_format=84
resource_pack_format_minor=0

# Sodium gained a config API in 0.8, so these are the targets whose Sodium build can show Continuity's
# options as a page of their own. The API artifact is compiled against; the mod itself is only added to
# the development run.
sodium_api_version=0.9.1+mc26.1.2
sodium_version=mc26.1.2-0.9.1-neoforge

# ModernFix can bake models on demand, and tells mods that ask through its integration entry point. It only
# ships for this target and 1.21.1, so only those compile the entry point. https://modrinth.com/mod/modernfix
modernfix_version=5.27.22+mc26.1.2
